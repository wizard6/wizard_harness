import { TRAJECTORY_VIEW_CSS, TRAJECTORY_VIEW_JS } from './view.js';

export const TRAJECTORY_HTML = [
  '<!doctype html><html lang="zh"><head><meta charset="utf-8"><style>',
  'html,body{height:100%;margin:0;overflow:hidden}',
  'body{display:flex;flex-direction:column;background:#0d1117;color:#e6e6ef;',
  'font:13px/1.5 system-ui,"Segoe UI","Microsoft YaHei",sans-serif}',
  '.bar{flex:none;padding:8px 14px;border-bottom:1px solid #262634;display:flex;gap:8px;align-items:center}',
  'h1{font:600 14px system-ui;margin:0}',
  'select{margin-left:auto;max-width:46%;background:#16161e;color:#e6e6ef;border:1px solid #333;',
  'border-radius:8px;padding:4px 8px;font:12px system-ui}',
  '#view{flex:1;min-height:0}',
  TRAJECTORY_VIEW_CSS,
  '</style></head><body>',
  '<div class="bar"><h1>执行轨迹</h1><select id="pick"><option value="">本轮</option></select></div>',
  '<div id="view" class="traj-root"></div>',
  '<script>',
  TRAJECTORY_VIEW_JS,
  'const view=document.getElementById("view"),pick=document.getElementById("pick");',
  'let lastKey="";',
  'async function refresh(force){',
  'if(!window.wh||!window.wh.call){view.innerHTML=\'<div class="traj-empty">窗口桥未就绪</div>\';return;}',
  'try{',
  'const listed=await window.wh.call("trajectory","list",[]);',
  'const items=(listed.ok&&listed.result)?listed.result:[];',
  'const cur=pick.value;',
  'const opts=[\'<option value="">本轮</option>\'].concat(items.map(function(it,i){',
  'const t=new Date(it.startedAt).toLocaleTimeString("zh-CN",{hour12:false});',
  'return \'<option value="\'+it.id+\'">#\'+(items.length-i)+" · "+t+" · "+it.spans+" 条</option>\';',
  '}).reverse());',
  'if(pick.innerHTML!==opts.join("")){pick.innerHTML=opts.join(""); if(cur) pick.value=cur;}',
  'const id=pick.value||undefined;',
  'const r=await window.wh.call("trajectory","snapshot",id?[id]:[]);',
  'const snap=r.ok?r.result:null;',
  'const key=snap?(snap.id+":"+(snap.spans?snap.spans.length:0)):"";',
  'if(!force&&key===lastKey)return;',
  'lastKey=key;',
  'if(!r.ok){view.innerHTML=\'<div class="traj-empty">\'+trajEsc(r.error||"读取失败")+"</div>";return;}',
  'renderTrajectory(view,snap);',
  '}catch(e){view.innerHTML=\'<div class="traj-empty">\'+trajEsc(e)+"</div>";}}',
  'pick.onchange=function(){lastKey="";refresh(true);};',
  'refresh(true);setInterval(function(){refresh(false);},800);',
  '</script></body></html>',
].join('');
