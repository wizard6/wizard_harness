# 骨架修复 · 人类确认文件

1. 打通观测链路：GUI 与 CLI/TUI/API 共用同一份 events.jsonl；Electron 装载 RegistryPanel，删平行 vanilla 渲染；四端统一消费 registrySpec。
2. plugins/ 新增 hello 插件，GUI 加载它，删内联 demo。
3. 契约健壮性：onStart 失败回滚；dependencies 未满足发警告；readEvents 缺文件返回 []；总线 sink 异常隔离；TUI 净额计数；移除 get/list/has 观测事件。
4. 文档卫生：确认文档路径改为 core/；vitest 覆盖扩到 obs；修 CLI types 入口；已实现草稿归档。
5. 边界：不改注册器已锁定契约与单测；不改 Electron 下载机制；不做插件发现/热插拔/权限/路由。
6. 流程：逐项先确认后修改，每项改完展示结果。
