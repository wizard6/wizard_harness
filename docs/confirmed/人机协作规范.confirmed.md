# 人机协作规范 · 人类确认文件

> 以下为示例草稿，请改写为你的真实意图；正文不超过 300 字，标题与说明不计入。

1. 类 deepseek-harness 的 Agent 基座：一切皆插件、一切可观测、人机共迭代。
2. 每份文档必经：AI 草稿 → 人类确认（≤300 字）→ 按确认实现，实现禁看草稿。
3. 规则文件同样按此流程进化；确认含糊时停下提问，不猜。
4. 插件须声明输入、输出、副作用、观测点；壳只做加载、事件总线、记录。
5. 先定规则与骨架，暂不做工具脚本，后续迭代再补。
6. 参考/知识类文档 AI 可直接读，无需确认环；确认环只用于意图类。
7. 单文件默认 ≤300 行，效果与内聚优先；超限须一句理由并计划再拆。
